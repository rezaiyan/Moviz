apply from: GradleDir.android
// In this module we want to add some base classes are shared in whole of the project
dependencies {
    testImplementation project(Modules.sdkTest)
    testImplementation TestLibraries.junit
    testImplementation TestLibraries.kluent
}
